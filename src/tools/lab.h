/* Every element on one page, through element.h alone. Not a screen: the gfx
 * check calls it as the one page whose contents are known ahead of time. */
#ifndef TOOLS_LAB_H
#define TOOLS_LAB_H

void lab_page_draw(void);

#endif
